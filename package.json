{
  "name": "test-turbo",
  "private": true,
  "scripts": {
    "build": "turbo build",
    "dev": "turbo dev",
    "lint": "turbo lint",
    "format": "turbo format"
  },
  "devDependencies": {
    "@ianvs/prettier-plugin-sort-imports": "^4.1.1",
    "@flux/eslint-config": "workspace:*",
    "@flux/typescript-config": "workspace:*",
    "conventional-changelog-cli": "^4.1.0",
    "husky": "^9.1.5",
    "prettier": "^3.1.0",
    "lint-staged": "^15.2.10",
    "turbo": "latest"
  },
  "lint-staged": {
    "*.{js,ts,jsx,tsx}": "eslint --fix",
    "**/*": "prettier -w -u"
  },
  "packageManager": "pnpm@8.15.7",
  "engines": {
    "node": ">=16"
  }
}
